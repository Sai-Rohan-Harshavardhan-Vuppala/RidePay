import { Typography } from "@mui/material";

const Page404 = () => {
  return (
    <div>
      <Typography>This page doesn't exist or you don't have access!</Typography>
    </div>
  );
};

export default Page404;
